<h1>Bonjour, <%= current_parent.email %> 👋</h1>

<h3 class="mt-4">👨‍👧 Enfants liés à votre compte :</h3>

<% if @kids.any? %>
  <% @kids.each do |kid| %>
    <div class="card mb-3">
      <div class="card-body">
        <h5 class="card-title"><%= kid.email %></h5>
        <p>
          Progression totale :
          <strong><%= kid.lesson_progresses.where(completed: true).count %></strong> leçons terminées
        </p>
      </div>
    </div>
  <% end %>
<% else %>
  <p>Aucun enfant lié à ce compte.</p>
<% end %>
